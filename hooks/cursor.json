{
  "version": 1,
  "hooks": {
    "sessionStart": [
      {
        "command": "node \"${CURSOR_PLUGIN_ROOT}/bin/agent-lcm\" capture --harness cursor sessionStart"
      }
    ],
    "beforeSubmitPrompt": [
      {
        "command": "node \"${CURSOR_PLUGIN_ROOT}/bin/agent-lcm\" capture --harness cursor beforeSubmitPrompt"
      }
    ],
    "postToolUse": [
      {
        "command": "node \"${CURSOR_PLUGIN_ROOT}/bin/agent-lcm\" capture --harness cursor postToolUse"
      }
    ],
    "stop": [
      {
        "command": "node \"${CURSOR_PLUGIN_ROOT}/bin/agent-lcm\" capture --harness cursor stop"
      }
    ]
  }
}
