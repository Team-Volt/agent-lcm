{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "agent-lcm": {
      "type": "stdio",
      "command": "node",
      "args": ["${PLUGIN_ROOT}/bin/agent-lcm", "mcp"]
    }
  }
}
